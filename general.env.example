# Operation mode: 'public' enables token generation, 'private' disables it
MODE="private"

# Comma-separated list of allowed domains for CORS proxy
ALLOWED_PROXY_DOMAINS="firstdomain.com,seconddomain.com"
